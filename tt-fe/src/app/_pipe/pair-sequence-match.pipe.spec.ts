import { PairSequenceMatchPipe } from './pair-sequence-match.pipe';

describe('PairSequenceMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new PairSequenceMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
